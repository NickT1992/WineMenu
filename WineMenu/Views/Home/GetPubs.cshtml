@*@model WineMenu.Models.Pub*@
@model IEnumerable<WineMenu.Models.Pub>

@{
    ViewBag.Title = "GetPubs";
}

<h2>GetPubs</h2>

@*<h2>Pubs- Last Updated: @Model.PubName</h2>*@

<ul>
    @foreach (var pub in Model)
    {
        <li>@Html.ActionLink(pub.PubName, "Browse", new { pub = pub.PubName })</li>
    }
</ul>

@*How to populate DDs from Controller*@
@*<h4>@ViewBag.Message</h4>
@Html.DropDownList("WineTypeDropDown", new SelectList(new [] {"White", "Red", "Rose"}))*@
